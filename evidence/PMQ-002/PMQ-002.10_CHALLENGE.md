# PMQ-002.10 — Durable Execution-State Rollback

## Status

FROZEN CHALLENGE — NOT YET EVALUATED

## Proposition

> Once a represented execution permit has been durably consumed and its initial execution outcome state durably established, restoration of an older durable-store snapshot MUST NOT make that same permit appear unused such that the represented consequence can be formed again without detection.

## Qualification starting point

This challenge is frozen on top of the PMQ-002.9 qualified evidence state (`c18e025f5519ffd4df1190b88b46f0d9d26b8f76`), whose full regression completed with 52 passed / 0 failed.

## Adversarial condition

The executable qualification should exercise the represented local persistence boundary by preserving a pre-execution durable-store state, allowing one valid execution to consume its permit and establish its execution outcome, restoring the older durable-store state, and then presenting the same permit/action binding again.

## Success condition

PASS only if the represented implementation detects the rollback or otherwise prevents the restored older state from enabling a second represented consequence.

If restoration makes the previously consumed permit appear unused and the same permit can form a second represented consequence, preserve the result as:

**FAIL — DURABLE STORE ROLLBACK RE-ENABLES CONSUMED PERMIT**

If the challenge does not reach the rollback condition because of fixture, time, authority-state or setup mismatch, classify the proposition as **NOT EVALUATED** and correct only the fixture/setup before rerun.

## Evidence discipline

- Proposition frozen before executable result.
- Do not weaken or replace the proposition after observing the result.
- Preserve the first genuine failure before remediation.
- No remediation before the initial executable result is recorded.
- Rerun the same proposition semantics after any remediation.
- Run the full maintained regression before final classification.

## Demonstrated scope if successful

A successful result would apply only to the represented reference-MVP persistence boundary and the rollback condition actually exercised.

## Explicitly not demonstrated by this challenge

- production backup/restore controls;
- storage IAM or administrative compromise resistance;
- database HA, replication or failover;
- cross-host, cross-server or multi-region rollback resistance;
- distributed consensus or serializability;
- filesystem, fsync, disk-controller or power-loss guarantees;
- immutable/write-once external audit anchoring;
- external monotonic counters or trusted ledgers;
- external payment-rail idempotency or settlement status;
- universal route closure;
- physical external consequence prevention.
