# PMQ-002.2 — Concurrent Store Failure

## Run

GitHub Actions run: `32160570769`

Result:

`1 failed, 43 passed`

## Observation

After correcting only the worker authority-state fixture, the two-process replay challenge reached the shared durable permit-consumption mechanism.

One worker failed inside `permit_consumption_store._connect()` with:

`sqlite3.OperationalError: database is locked`

The lock occurred while both independently spawned processes attempted concurrent SQLite store initialization / `PRAGMA journal_mode=WAL` setup.

Because one worker crashed before returning an execution outcome, the frozen replay proposition itself was not conclusively evaluated. The run did **not** demonstrate two consequences forming, and it must not be labelled as such.

## Classification

**NOT EVALUATED — MULTI-INSTANCE REPLAY PROPOSITION NOT CONCLUSIVELY REACHED**

Additional observed defect:

**FAIL — CONCURRENT DURABLE-STORE INITIALIZATION IS NOT ROBUST UNDER THE TESTED TWO-PROCESS RACE**

## Why this matters

A replay-control mechanism that depends on shared durable state must tolerate credible concurrent access. A worker crash caused by store initialization contention is a real weakness even though it is not evidence that duplicate consequences formed.

## Remediation constraint

Strengthen only the durable-store connection/initialization behaviour so concurrent processes can reach the atomic permit-consumption operation reliably.

Then rerun the same PMQ-002.2 replay proposition and the same two-process race semantics.

Do not weaken the PASS condition:

- exactly one `CONSEQUENCE_FORMED`;
- no more than one represented consequence;
- the competing use must be refused as replay.