# PMQ-002.2 — Initial Run Note

## Initial run

GitHub Actions run: `32160449500`

Result:

`1 failed, 43 passed`

The PMQ-002.2 assertion failed because **neither** worker formed a consequence. Both independently spawned executor processes returned:

`DENIED_AUTHORITY_STATE_STALE`

Observed outcomes:

`['DENIED_AUTHORITY_STATE_STALE', 'DENIED_AUTHORITY_STATE_STALE']`

## Classification

**NOT EVALUATED — MULTI-INSTANCE REPLAY PROPOSITION NOT REACHED**

This is not classified as a product/invariant failure and is not classified as a PASS.

The reason is test-fixture state divergence: the parent pytest process had an authority-state version affected by earlier regression tests, while each independently spawned worker process initialized its own in-memory authority store at version `1`. The permit therefore failed current-standing validation before the durable replay-consumption race was reached.

## Required correction

Correct only the test fixture so that each represented executor process begins with the same authority-state version carried by the already-issued permit.

The frozen proposition, concurrency model, exact permit, exact action binding, PASS condition and FAIL condition remain unchanged.

No replay-control remediation is permitted before the corrected test reaches the proposition.