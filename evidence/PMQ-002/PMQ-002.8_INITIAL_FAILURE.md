# PMQ-002.8 — Initial Failure Record

## Status

**PENDING EXECUTABLE CI CONFIRMATION**

The frozen PMQ-002.8 test has been added without changing the runtime implementation.

The current architecture records permit consumption before invoking the pre-formation hook. PMQ-002.7 can record `CONSEQUENCE_NOT_FORMED` when that hook raises a catchable exception, but an abrupt `os._exit` termination bypasses that exception handler. No durable unresolved outcome is written before the crash point.

The expected pre-remediation classification, if confirmed by CI, is:

**FAIL — HARD CRASH LEAVES CONSUMED PERMIT WITH NO EXPLICIT OUTCOME STATE**

This file does not substitute expectation for test evidence. The exact CI result must be recorded after the frozen challenge runs.